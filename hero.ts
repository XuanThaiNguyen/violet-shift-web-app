// hero.ts
import { commonColors, heroui } from "@heroui/react";
export default heroui({
    themes: {
        light: {
            colors: {
                background: commonColors.zinc[50],
            }
        }
    }
});